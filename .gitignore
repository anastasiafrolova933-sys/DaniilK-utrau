# Лог автообновления — локальный, не публикуется
data/update.log

# Локальный конфиг превью
.claude/

# Бэкенд чата/push — серверный, с секретами (.env, VAPID, ключ API), НЕ публикуется
backend/

# Системные
Thumbs.db
.DS_Store

# Промежуточный XLSX СПА (пересоздаётся build_spa.py)
data/spa_raw.xlsx

# Промежуточные XLSX F&B по месяцам (пересоздаются build_fnb.py)
data/fnb_raw/

# Python-байткод
scripts/__pycache__/
